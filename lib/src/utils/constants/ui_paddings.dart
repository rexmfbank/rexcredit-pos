import 'package:flutter/material.dart' show EdgeInsets;

abstract class UiPaddings {
  /// a padding of EdgeInsets.only(left:16, right:16)
  static const padding16LR = EdgeInsets.symmetric(horizontal: 16.0);
}
